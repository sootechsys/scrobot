<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="view010101P02">

	<select id="retrieveWrkDetail" parameterType="hashmap" resultType ="egovMap">

	    	/* retrieveWrkList */
	    	
			SELECT WRK_NM
			     , SOURCE
			FROM ROBOT02
			WHERE USER_ID = #{userId}
			<if test='wrkNm != "" and wrkNm != anull'>
			AND WRK_NM = #{wrkNm}    
			</if>
			AND CHNG_HISTRY_DTTM = #{chngHistryDttm}
	</select>

</mapper>